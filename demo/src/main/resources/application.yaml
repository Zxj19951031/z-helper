swagger2:
  apiInfo:
    title: Title
    version: 1.0
    description: Swagger-UI
    contact:
      name: zhuxj
      url: http://127.0.0.1
      email:
  apis:
    basePackage: org.zipper.demo.zdemo
logging:
  level:
    org.zipper.demo.zdemo: debug
    root: info

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/z_helper_auth?useSSL=false&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver